import { StyleSheet } from "react-native";

export const trendStyle = StyleSheet.create({

  layout: {
    width: "100%",
    flexDirection: "row",
    justifyContent: "space-between",
    marginBottom: 15
  },

  title: {
    includeFontPadding: false,
    fontSize: 15,
    fontWeight: 700
  }
})