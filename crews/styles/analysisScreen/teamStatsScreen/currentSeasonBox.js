import { StyleSheet } from "react-native";

export const currentBoxStyle = StyleSheet.create({
    layout: {
        borderColor:"#E5E9EF",
        padding:15,
        borderWidth:1,
        borderRadius:13,
        marginLeft:15,
        marginRight:15,
      },
})