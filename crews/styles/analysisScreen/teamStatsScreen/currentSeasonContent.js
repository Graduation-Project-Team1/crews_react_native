import { StyleSheet } from "react-native"

export const currentContentStyle = StyleSheet.create({
    textBold: {
        fontWeight:'bold'
    },
    layout:{
        flexDirection: 'row', 
        justifyContent: 'space-around', 
        marginTop: 7 
    }
})
