import { StyleSheet,Color } from "react-native";

export const positionTextStyle = StyleSheet.create({
    layout:{
        marginTop:20,
        marginLeft:10,
        fontSize:17,
        fontWeight:'bold'
    }
})